import '../css/Main.css';
import ProductList from '../ItemDetail/ItemDetail';


const getItems = () => {
    
    setTimeout(() => {
        ItemList !== [] ? resolve(ItemList) : reject('No hay productos')
    }, 2000);
};